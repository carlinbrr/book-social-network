spring:
  datasource:
    url: ${POSTGRESQL_DB_URL}
    username: ${POSTGRESQL_DB_USERNAME}
    password: ${POSTGRESQL_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: ${EMAIL_HOSTNAME}
    port: 465
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
            trust: "*"
        starttls:
          enable: true
    protocol: smtp

application:
  security:
    jwt:
      secret-key: 7e9778b3562564d23f3c5937c32a9b178c386c34842a564332d7e193cea2a71a
      expiration: 86400000
  mailing:
    frontend:
      activation-url: ${frontend.http.url}/activate-account
  file:
    upload:
      photos-output-path: ./uploads

server:
  port: 8088

frontend:
  http:
    url: ${FRONTEND_HTTP_URL}
